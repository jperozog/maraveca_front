import { FiltradoInvetnarioPipe } from './filtrado-invetnario.pipe';

describe('FiltradoInvetnarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltradoInvetnarioPipe();
    expect(pipe).toBeTruthy();
  });
});
